# プロジェクト管理 (Projects) 画面仕様

## 1. 概要

営業プロジェクト（商材×ターゲット戦略）の設定と、企業規模別のアプローチ分岐を管理する画面。

---

## 2. 画面一覧

| 画面 | パス | 説明 |
|------|------|------|
| プロジェクト一覧 | `/projects` | 全プロジェクトのリスト |
| プロジェクト詳細 | `/projects/[id]` | 設定・テンプレート管理 |
| プロジェクト作成 | `/projects/new` | 新規プロジェクト作成 |

---

## 3. プロジェクト一覧画面

### 3.1. 画面構成

```
┌─────────────────────────────────────────────────────────────┐
│  プロジェクト管理                        [+ 新規プロジェクト] │
├─────────────────────────────────────────────────────────────┤
│  🔍 検索                          ステータス: [すべて ▼]    │
├─────────────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────────────┐  │
│  │ □ │ プロジェクト名 │ 商材      │ ターゲット数│ ステータス│操作│
│  ├───┼───────────────┼───────────┼────────────┼─────────┼────┤
│  │ □ │ SaaS営業2024Q1│ クラウドCRM│     245    │ Active  │ ⋮  │
│  │ □ │ 金融業界開拓  │ セキュリティ│     128    │ Active  │ ⋮  │
│  │ □ │ 製造業DX推進  │ IoTセンサー │      89    │Archived │ ⋮  │
│  └───────────────────────────────────────────────────────┘  │
│                                                             │
│  ← 1 2 3 ... 10 →                                          │
└─────────────────────────────────────────────────────────────┘
```

### 3.2. テーブルカラム定義

| カラム | 型 | ソート | フィルタ | 説明 |
|--------|-----|:------:|:-------:|------|
| チェックボックス | Checkbox | - | - | 一括操作用 |
| プロジェクト名 | Link | ✓ | - | クリックで詳細へ |
| 商材 | Text | - | - | `product_description` の要約 |
| ターゲット数 | Number | ✓ | - | `targets` テーブルのカウント |
| ステータス | Badge | - | ✓ | `active` / `archived` |
| 操作 | Dropdown | - | - | 編集・アーカイブ・削除 |

### 3.3. 操作メニュー

| 操作 | 説明 |
|------|------|
| 編集 | プロジェクト詳細画面へ遷移 |
| アーカイブ | ステータスを `archived` に変更 |
| 削除 | 確認ダイアログ後に削除 |

---

## 4. プロジェクト詳細画面

### 4.1. 画面構成

```
┌─────────────────────────────────────────────────────────────┐
│  ← プロジェクト一覧   SaaS営業2024Q1                 [保存]  │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─ 基本情報 ─────────────────────────────────────────────┐ │
│  │  プロジェクト名: [SaaS営業2024Q1              ]        │ │
│  │  ステータス:     [Active ▼]                            │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                             │
│  ┌─ 商材情報 ─────────────────────────────────────────────┐ │
│  │  [                                                    ]│ │
│  │  [  クラウド型CRMシステム。月額5万円から導入可能。    ]│ │
│  │  [  導入企業は平均30%の営業効率改善を実現...          ]│ │
│  │  [                                                    ]│ │
│  └────────────────────────────────────────────────────────┘ │
│                                                             │
│  ┌─ ターゲットペルソナ ───────────────────────────────────┐ │
│  │  [                                                    ]│ │
│  │  [  従業員50-500名の中堅企業。営業部門の責任者。      ]│ │
│  │  [  既存のExcel管理に課題を感じている企業...          ]│ │
│  │  [                                                    ]│ │
│  └────────────────────────────────────────────────────────┘ │
│                                                             │
│  ┌─ アプローチ分岐設定 ───────────────────────────────────┐ │
│  │  売上閾値: [10億円 ▼]                                  │ │
│  │                                                        │ │
│  │  • 10億円未満  → Mass型（LP誘導）                     │ │
│  │  • 10億円以上  → Account型（個別対応）                │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                             │
│  ┌─ 文面パターン ─────────────────────────────────────────┐ │
│  │  [+ 新規テンプレート]                                  │ │
│  │  ┌─────────────────────────────────────────────────┐  │ │
│  │  │ タイプ  │ テンプレート名      │ 操作           │  │ │
│  │  ├─────────┼────────────────────┼────────────────┤  │ │
│  │  │ Mass    │ LP誘導_標準        │ 編集 | 削除    │  │ │
│  │  │ Account │ 課題ヒアリング_標準 │ 編集 | 削除    │  │ │
│  │  └─────────────────────────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### 4.2. セクション詳細

#### 基本情報

| フィールド | 型 | 必須 | 説明 |
|------------|-----|:----:|------|
| プロジェクト名 | Input | ✓ | 最大100文字 |
| ステータス | Select | ✓ | `active` / `archived` |

#### 商材情報

| フィールド | 型 | 必須 | 説明 |
|------------|-----|:----:|------|
| 商材情報 | Textarea | - | AIプロンプトに使用される商材説明 |

#### ターゲットペルソナ

| フィールド | 型 | 必須 | 説明 |
|------------|-----|:----:|------|
| ターゲットペルソナ | Textarea | - | 想定顧客像の説明 |

#### アプローチ分岐設定

| フィールド | 型 | 必須 | 説明 |
|------------|-----|:----:|------|
| 売上閾値 | Select | ✓ | 1億/5億/10億/50億/100億円 |

#### 文面パターン

テンプレート一覧をテーブル表示。

| カラム | 説明 |
|--------|------|
| タイプ | `mass` / `account` |
| テンプレート名 | テンプレートの識別名 |
| 操作 | 編集・削除ボタン |

---

## 5. テンプレート編集モーダル

### 5.1. 画面構成

```
┌─────────────────────────────────────────────────────────────┐
│  文面テンプレート編集                               [×]    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  テンプレート名: [LP誘導_標準                      ]        │
│  タイプ:        [Mass ▼]                                   │
│                                                             │
│  件名テンプレート:                                          │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ {{company_name}}様｜業務効率化のご提案             │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  本文テンプレート:                                          │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ {{contact_name}}様                                   │   │
│  │                                                      │   │
│  │ 突然のご連絡失礼いたします。                        │   │
│  │ {{product_description}}                              │   │
│  │                                                      │   │
│  │ 詳細はこちらをご覧ください: {{lp_url}}             │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  💡 使用可能な変数:                                        │
│  {{company_name}}, {{contact_name}}, {{industry}},         │
│  {{product_description}}, {{lp_url}}                       │
│                                                             │
│                               [キャンセル]  [保存]         │
└─────────────────────────────────────────────────────────────┘
```

### 5.2. 使用可能な変数

| 変数 | 説明 |
|------|------|
| `{{company_name}}` | 企業名 |
| `{{contact_name}}` | 担当者名 |
| `{{industry}}` | 業界 |
| `{{product_description}}` | 商材情報 |
| `{{lp_url}}` | LPのURL（プロジェクト設定で指定） |

---

## 6. データ操作

### 6.1. プロジェクト作成

```typescript
const createProject = async (data: ProjectInput) => {
  const { data: project, error } = await supabase
    .from('projects')
    .insert({
      name: data.name,
      product_description: data.productDescription,
      target_persona: data.targetPersona,
      strategy_threshold: data.strategyThreshold,
      created_by: currentUser.id,
    })
    .select()
    .single();

  return project;
};
```

### 6.2. テンプレート作成

```typescript
const createTemplate = async (projectId: string, data: TemplateInput) => {
  const { data: template, error } = await supabase
    .from('draft_templates')
    .insert({
      project_id: projectId,
      type: data.type,
      name: data.name,
      subject_template: data.subjectTemplate,
      body_template: data.bodyTemplate,
    })
    .select()
    .single();

  return template;
};
```

---

## 関連ドキュメント

- [全体要件定義書](../../overview/requirements.md)
- [データベース設計](../../technical/database-schema.md)
- [ターゲットリスト画面](./targets.md)
